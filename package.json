{
  "name": "your-monorepo",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0",
    "concurrently": "^7.0.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "concurrently \"lerna run start --scope back-end\" \"lerna run start --scope front-end\"",
    "front": "lerna run start --scope back-end",
    "back": "lerna run start --scope front-end"
  }
}
